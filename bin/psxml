#!/usr/bin/ruby

$: << File.expand_path(File.dirname(__FILE__) + "/../lib")

require 'psxmlservlet.rb'


# main like
server = HTTPServer.new({:Port => 1080})

server.mount("/",PSXMLServlet,nil)

trap("INT"){server.shutdown}
server.start
